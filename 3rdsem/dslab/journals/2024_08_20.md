# Linked-List

```c
struct Node{
    int a;
    char c;
    struct node* Next;
};

struct node* n1 = (struct Node*) malloc(sizeof(struct Node);
n1 -> a = 90;
n1 -> c = "M";
n1 -> next = NULL;


```

## Tasks
