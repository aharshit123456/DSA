

int main()
{
    int n = 10;

    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < n - i - 1; j++)
        {
            if (a[j] > a[j + 1])

                swap(a[j], a[j + 1]);
            flag = True;)
        }
    }
    if (flag = True)
        return 0
}
}